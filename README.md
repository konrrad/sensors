- you can easily build the app with gradle
- the app starts by default on port 8080
- the documentation of the api is generated by swagger and is available [here](http://localhost:8080/swagger-ui.html)
- it uses an in-memory database
- there are some tests written